# The Unix Gradle wrapper must stay LF and executable on all platforms.
gradlew text eol=lf
*.bat text eol=crlf
# Git hooks are shell scripts run by sh; keep them LF on every platform.
.githooks/* text eol=lf
